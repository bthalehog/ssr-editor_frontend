{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "dev:rollup": "rollup -c -w",
    "build:rollup": "rollup -c",
    "startsirv": "sirv public --no-clear --host 0.0.0.0",
    "deploy": "npm run build && node ./scripts/gh-pages.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.39.6",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "@sveltejs/adapter-static": "^3.0.10"
  }
}
